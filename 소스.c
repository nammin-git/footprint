#include <stdio.h>

int main(void)
{/*
	//정수연산
	int x, y, result;

	printf("두 개의 정수를 입력하시오: ");
	scanf_s("%d %d", &x, &y);

	result = x + y;
	printf("%d + %d = %d\n", x, y, result);

	result = x - y;
	printf("%d - %d = %d\n", x, y, result);

	result = x * y;
	printf("%d * %d = %d\n", x, y, result);

	result = x / y;
	printf("%d / %d = %d\n", x, y, result);

	result = x % y;
	printf("%d %% %d = %d\n", x, y, result);


	//실수 연산
	double x, y, result;

	printf("두 개의 실수를 입력하시오: ");
	scanf_s("%lf %lf", &x, &y);

	result = x + y;
	printf("%lf + %lf = %lf\n", x, y, result);

	result = x - y;
	printf("%lf - %lf = %lf\n", x, y, result);

	result = x * y;
	printf("%lf * %lf = %lf\n", x, y, result);

	result = x / y;
	printf("%lf / %lf = %lf\n", x, y, result);
	*/

	//A/B
	double a = 1;
	double b = 3;
	double result;

	a = 1;
	b = 3;
	result = a / b;

	printf("%lf", result);

	return 0;
}